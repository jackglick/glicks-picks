---
name: preview
description: Open the site in a browser for visual review
disable-model-invocation: true
---

Open the site for local preview:

1. Open index.html in the default browser: `open index.html`
2. Report which changes were made since the last commit using `git diff --stat`
3. Remind the user to check responsive layouts at mobile (375px), tablet (768px), and desktop (1024px) widths
